<h1><%= t('groups.show.title') %></h1>
<h3><%= t('groups.show.group_name') %></h3>
<%= @group.group_name %>
<h3><%= t('groups.show.group_users') %></h3>
<%# <%= @group.users.each do |user| %> %>
<%# user.tasks.each do |task| %>
<%# link_to task.task_name, task_path(task.id) %>
